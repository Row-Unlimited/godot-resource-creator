TODOs:
	- Implement typed dicts
		+ change it so container doesn't spawn button if the type is string/float/int but just a regular line-edit
		+ implement receive_input for keys
		+ Implement classconfigs so users can define how keys should be handled
	- Redesign object input UI:
		+ remove x button and just make the confirm button a toggle if object isn't typed(unsure?)
		+ find way of implementing assigning other objects to the object input, maybe by reference, by duplicate or maybe make both possible
		+ remove redundant chosen script label that shows what is already written in select, probably replace it with a button that opens the select object menu described directly above
	- Make typed the type select for arrays and dicts be grayed out if it's typed instead of just allowing a single type
	- Add duplicate button in overview menu so people can duplicate objects and then edit the copy
	- Implement popup for deleting objects
	- Implement variable in plugin_config to disable popups for quicker navigation
	- Implement more formatting for the comment tooltips
	- UX:
		+ show class_config modifications in some way in the creation window when hovering over a property for example
		+ Add Path selection option-button to create_object and add my line-edit plugin to the line-edit in the export menu
	- UI:
		+ More UI Improvements like changing the tabBar node
	- Integration:
		+ Come up with integration features
	- rename to godot_resource_creator
	- Allow integration of overview menu:
		+ add functions that create a create_object window without displaying it, to allow integration without swapping the main-screen

Future TODOs:
	- Implement Option into Object_Input to use a created wrapper to assign it to multiple values.
	- Implement Color Input type
	- Code refactoring:
		+ Move Vector match statements into TypeManager
		+ Redo how vectors apply sub_configs, so that they have a X_CONFIG property that gets written into directly instead of doing all these shenanigans.

BUG LOG:
	- Object inputs in a typed array with type e.g. Array[TestScript] will show "Resource" in type UI label. It should be TestScript tho
	- pressing on dict creates highlight effect that is somehow still there when changing tabs
	- Margin still exists for Vector types in array which should be changed