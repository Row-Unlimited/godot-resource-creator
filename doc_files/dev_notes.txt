TODOs:
	- Implement config for dictionaries
	- Fix config of the sub configs, so they actually apply and throw errors
	- UX:
		+ make it consistent when the screen changes automatically
			~ first object created switches to the creation screen, subsequent objects not
			~ maybe make it switch every time we use the left mouse button, and make it possible to create one without switching with the middle mouse button
		+ Configure the neighbors correctly for easier navigation with tab
			~ maybe add other shortcuts like moving array elements with arrow keys
	- UI:
		+ More UI Improvements like changing the tabBar node
		+ Change Array order elements (maybe add dragging)
	- Integration:
		+ Come up with integration features and remove old unused vars etc.
	- rename to godot_resource_creator
	Allow integration of overview menu

Future TODOs:
	- Implement Color Input type
	- Code refactoring:
		+ Move Vector match statements into TypeManager
		+ Redo how vectors apply sub_configs, so that they have a X_CONFIG property that gets written into directly instead of doing all these shenanigans.
		+ Create script that searches for unused functions and remove those